#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

#include "../../graphExecution/graph.h"

#define MAX_NUM_DIGITS 1000

void solve(char* a, char* b, char* ans) {
}

int main() {
    while (1) {
        char* a = (char*) malloc(sizeof(char) * MAX_NUM_DIGITS);
        char* b = (char*) malloc(sizeof(char) * MAX_NUM_DIGITS);

        printf("Input a number: ");
        scanf("%s", a);
        printf("Input another number: ");
        scanf("%s", b);


   }
}